# Caveman flat mode comparison test
# Phase 1: 4 turns without caveman (baseline), measuring context growth
# Phase 2: clear + enable ultra caveman + same 4 turns, measuring context growth
# The token report will show lower orchestrator_prompt_max in phase 2 turns

/mode
/mode
/flat

# --- Phase 1: Baseline (caveman off) ---

# Turn 1: read types.rs and describe every public type
Read src/types.rs and list every public type defined there — for each one, state its name, kind (enum/struct/type alias), and all its fields or variants.
@wait 90

# Turn 2: read app.rs and describe App struct and all its fields
Read src/app.rs and describe in detail the App struct: every field name, its type, and what it is used for based on the surrounding code.
@wait 90

# Turn 3: read session.rs and explain all public methods on Session
Read src/session.rs and explain every public method on the Session struct: what each method does, its parameters, and return type.
@wait 90

# Turn 4: write to verify context is still usable after 3 accumulated turns
Write "baseline_done" to caveman_flat_baseline.txt
@wait 60
@expect_file caveman_flat_baseline.txt baseline_done

# Note orchestrator_prompt_max at steps 0-3 — this is the accumulated flat context without caveman.

# --- Phase 2: Ultra caveman (fresh conversation) ---
/clear

# Enable ultra caveman (3 toggles = ultra)
/caveman
/caveman
/caveman

# Turn 5: same task as Turn 1
Read src/types.rs and list every public type defined there — for each one, state its name, kind (enum/struct/type alias), and all its fields or variants.
@wait 90

# Turn 6: same task as Turn 2
Read src/app.rs and describe in detail the App struct: every field name, its type, and what it is used for based on the surrounding code.
@wait 90

# Turn 7: same task as Turn 3
Read src/session.rs and explain every public method on the Session struct: what each method does, its parameters, and return type.
@wait 90

# Turn 8: write to verify context is still usable after 3 accumulated caveman turns
Write "caveman_done" to caveman_flat_ultra.txt
@wait 60
@expect_file caveman_flat_ultra.txt caveman_done

# Compare orchestrator_prompt_max at steps 4-7 vs steps 0-3.
# Ultra caveman emits terse tool results and compressed responses, so accumulated
# flat context grows more slowly — steps 4-7 should show noticeably lower values.
